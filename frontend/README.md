# 数据库AI自动化运维平台（前端）

前端为 Vue 3 + Vite + Element Plus。

## 本地运行（Windows / Linux / macOS）

```bash
cd frontend
npm install
npm run dev
```

默认开发端口：`http://localhost:3000/`

## 后端代理

开发环境通过 Vite 代理 `/api` 到后端：

- 代理配置：`vite.config.js`
