# 验证记录

## 当前预览状态

已在预览地址验证未登录访问。控制台正确显示蓝图风格的受控访问页面，并明确要求完成安全登录后才能访问数据库资产、执行单和审计信息。该门禁符合运维平台对敏感信息与生产操作的访问控制要求。

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| TypeScript 类型检查 | 通过 | `pnpm check` 无错误。 |
| 单元测试 | 通过 | 23 个测试文件、49 个测试通过，覆盖认证、本地初始化管理员、14 类引擎能力矩阵、受控执行、监控回调、撤销重试及智能 Runbook→执行编排。 |
| 生产构建 | 通过 | `pnpm build` 完成前后端打包；构建工具提示前端主包较大，后续可按页面拆分代码以优化首屏加载。 |
| 未登录访问 | 通过 | 显示安全登录入口，未渲染资产与执行数据。 |
| 已登录控制台页面 | 通过 | 在有效 OAuth 管理员会话下打开总览、资产、Runbook、集成与执行节点、智能处置及能力矩阵；关键查询均返回 HTTP 200，空状态准确显示为 0 或“—”。 |

后续生产联调还需要提供受控执行节点的网络地址、双向认证材料和目标数据库网络连通性，以及 Zabbix、Prometheus、XXL-Job 的实际端点与密钥引用；在这些条件齐备前，控制面不会将任何生产变更直接下发到数据库。

## 本地初始化管理员认证

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 安全配置凭据 | 通过 | 初始化用户名与临时密码仅以服务端环境变量提供，未回显到日志、页面或测试输出。 |
| 首次本地登录 | 通过 | 运行中端点使用初始化凭据响应 200，并返回 `mustChangePassword: true`。 |
| 强制改密门禁 | 通过 | 首次会话只能查询改密状态；受保护的运维路由会统一返回首次改密提示。 |
| 会话轮换 | 通过 | 成功改密会签发新 Cookie 与新会话版本，旧版本被拒绝；新会话可调用受保护的 Runbook 目录接口。 |
| 全量回归 | 通过 | `pnpm check`、`pnpm test`（23 个测试文件、49 个测试）和 `pnpm build` 均已通过。 |

## Runbook 中心商业 SaaS 重构

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 深色主题 | 通过 | 控制台主背景、卡片与边框统一为 #0F172A、#1E293B、#334155，取消大面积蓝图网格。 |
| 指标与目录 | 通过 | 指标栏显示真实目录中的 14 个标准模板；目录提供五类筛选、风险微光标签、数据库图标与悬浮快捷操作。 |
| 受控执行工作区 | 通过 | 未选模板时显示最近执行动态；选中后平滑打开包含实例、节点、参数、立即/计划执行和审批人的表单。 |
| 审计数据表 | 通过 | 重构为含状态、执行单、类型/风险、实例、触发源、时间与日志/撤销/重试操作的响应式企业级表格。 |
| 响应式与构建 | 通过 | 已验证桌面和 375px 移动端布局；`pnpm check`、49 项测试及生产构建均通过。 |

运行指标始终从执行记录实时计算；在没有实际执行单的数据环境中会显示 0 或 “—”，不会展示虚构的运维数据。计划执行单会保存为 `scheduled` 状态并避免立即派发；其到期调度需在生产部署后连接托管 Heartbeat 或已配置的 XXL-Job 工作流。

## 执行单操作闭环

撤销和重试已不再是前端占位操作。控制面会拒绝撤销已完成任务，仅允许失败或已撤销的执行单重试；重试单会复制已审阅的执行参数并记录其原始执行单。表格从持久化的 `triggerSource` 显示手动、故障自愈、计划调度或重试来源。智能处置现在以服务端编排连续创建激活 Runbook 和执行单，自动化测试验证了返回的 Runbook 标识与 `incident_auto` 被持久化。当前共 14 个测试文件、36 项测试均通过。

## 已登录页面验收（2026-08-27）

在有效的 OAuth 管理员会话下，桌面端完整截图检查了 `/`、`/assets`、`/runbooks`、`/integrations`、`/intelligence` 与 `/capabilities`。页面均可加载并呈现一致的深色 SaaS 视觉层级；Runbook 中心展示 14 个模板、风险微光标签、真实零值指标、最近执行空状态及企业审计表。额外在 375px 宽度检查 `/runbooks`，指标、分类、模板操作和执行追踪保持单列可读且没有横向溢出。验收期间对应的认证和运维 tRPC 批量请求均返回 200，未发现影响页面的前端控制台异常。首次改密接口也已要求当前临时密码；缺失该字段将被路由拒绝且不会进入改密服务。

生产联调仍须在真实内网执行节点、目标数据库的最小权限账户、监控平台端点及回调密钥引用配置齐备后进行。计划执行已确保不会被节点提前领取；应在发布后的托管 Heartbeat 或已登记 XXL-Job 中以幂等任务将到期任务转换为可领取或待审批状态，不能使用 Web 进程内定时器。

### Runbook 交互证据

在已登录浏览器会话中，目录接口返回 14 个适配器与 14 个标准 Runbook。依次切换“实例部署、备份恢复、故障自愈、性能巡检、全部”筛选后，选择“一键执行”模板入口；页面已展开目标数据库实例与受控执行配置区域。该操作仅验证筛选与表单状态，不提交执行单，不会改变数据库或生产环境。

资产中心已选择 MySQL 类型筛选，筛选控件状态随操作正确更新；当前环境未登记资产，因此列表保留与筛选条件一致的安全空状态。该操作没有创建、修改或删除资产。

集成与执行节点页面的“登记集成入口”表单在缺少集成名称时触发浏览器原生必填校验并停止提交；未产生新的集成记录、执行节点或外部网络请求。

智能处置页面在未输入诊断上下文时仅呈现受保护的生成入口和空分析状态，不存在“创建并执行”草案动作。为避免消耗模型调用配额或生成未经用户确认的运维建议，本轮未提交 AI 诊断请求；页面文案明确提示模型仅会在用户主动点击后调用，且任何高风险动作均需人工确认。

## Bytebase / Yearning 集成与服务器资产验收（2026-08-27）

服务器资产入口已加入控制台侧边栏并注册 `/servers` 路由。桌面端验证了指标栏、服务器清单、SQL 审核闸门、变更工单表和空数据状态；375px 移动端验证了菜单、指标卡、表单和表格容器的纵向布局，SQL 编辑区和提交按钮保持可访问。页面不会展示虚构资产、探活或工单数据，均以 0 或安全空状态呈现。

服务器资产现支持登记、详情读取和“请求受控探活”。请求只更新排队时间与提示，不执行 SSH、数据库 CLI 或其他厂商命令；真实状态必须由内网受控执行节点通过后续安全回报更新。SQL 审核现支持空 SQL、DROP/TRUNCATE、无 WHERE 的 UPDATE/DELETE、SELECT * 等内置规则，并可创建待审核/阻断变更工单；查询审计接口只保存 SQL 哈希和审计摘要，不执行查询。

本轮截图日志中出现一次已过期 JWT 的历史认证失败记录，随后 Vite HMR 与页面构建正常；未将该历史会话噪声视为新增功能代码错误。生产发布前仍需由负责人在管理界面点击 Publish，并在真实受控节点、密钥引用和目标数据库环境中联调探活与工单执行。

## 本地管理员密码恢复验收（2026-08-27）

用户明确授权后，系统使用已配置的初始化恢复凭据执行一次受控恢复。恢复接口返回 HTTP 200；账户随后进入强制改密门禁，数据库非敏感状态显示 `mustChangePassword=1` 且会话版本递增。用户完成新密码设置后，浏览器访问 `/servers` 已进入受保护控制台，数据库状态恢复为 `mustChangePassword=0`，会话版本再次递增至当前值。初始化密码和新密码均未写入代码、日志或本记录。

恢复实现仅允许“已存在本地账户 + 配置的初始化用户名和初始化密码”进入恢复分支，恢复后立即递增会话版本并重新要求改密；正常账户密码校验仍保持不变。自动化测试覆盖恢复判定、完整领域流程、旧密码失效、缺少当前密码拒绝和旧会话 Cookie 在改密路由前被认证层拒绝。当前认证专项测试全部通过。

## 服务器资产与治理工作台回归（2026-08-27）

新增服务器资产工作台已完成桌面与 375px 移动端视觉验收。菜单、`/servers` 路由、资产登记、资产详情、受控探活请求、SQL 规则初检、变更审核工单及查询审计摘要均有对应服务端契约。服务器生命周期测试覆盖登记、列表、详情、探活排队和受控节点回报状态更新；SQL 治理测试覆盖危险 DDL、无 WHERE DML、SELECT * 警告和正常变更。

当前集成采用“控制面治理 + 受控节点执行”边界：控制面不保存明文数据库/服务器密码、不在 Web 进程执行 SSH 或数据库 CLI、不直接执行审核 SQL。SQL 审核策略写入和变更工单管理员审批受 `adminProcedure` 保护，查询审计只记录 SQL 哈希与审计摘要。数据库元数据通过受控同步入口保存版本、对象摘要和最后同步时间；探活请求只排队，执行节点必须通过独立执行网关共享密钥、绑定服务器资产后回传状态。

服务器资产工作台截图验收：桌面端显示“服务器资产”侧栏入口、四项真实零值指标、服务器清单、SQL 审核闸门和变更/查询审计表；移动端 375px 下指标单列堆叠，资产清单、SQL 审核表单和审计区均保持可读，无需伪造运行数据。当前无真实服务器资产，因此详情/关联实例/执行节点区域仅在选择真实记录后显示，空状态明确提示。

最新浏览器交互验收：已登录会话打开 `/servers`，侧栏“服务器资产”处于选中状态，页面显示真实空数据指标和治理表单；点击“登记服务器”后登记表单正常展开，展示资产名称、主机名、IP、操作系统、区域、负责人、密钥引用和能力标签字段。此次仅打开表单，未保存任何资产，未产生写操作或敏感凭据。

补充交互验收：服务器登记表单可取消并返回真实空状态；在 SQL 审核文本框输入仅用于预览的 `DROP TABLE audit_probe_only` 后，页面即时显示“审核发现需处理项”，未点击创建工单，未写入数据库或触发生产执行。当前无真实服务器记录，故详情展开与探活按钮没有可供选择的目标；其服务端生命周期、节点绑定和回传接口已由 Vitest 覆盖。

## GitHub 推送记录（2026-08-27）

本次更新已安全推送至私有仓库 `wangzhenye2019/db-ai-ops-platform` 的功能分支 `manus/db-control-enhancement`，远程提交为 `ad6857b`（`chore: sync governance integration checklist`），并已通过 fetch 核对远程分支 HEAD 与本地 HEAD 一致、工作区干净。由于远程 `main` 与当前项目历史无共同祖先，直接推送 main 被 Git 拒绝；为避免覆盖远程已有 23 个提交，未使用强制推送，改用独立功能分支供后续 Pull Request 审阅合并。Pull Request 入口：<https://github.com/wangzhenye2019/db-ai-ops-platform/pull/new/manus/db-control-enhancement>。
