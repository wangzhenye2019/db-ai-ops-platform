# 发布与验收指引

当前发布候选版本为 `120ec85f`，已完成深色商业 SaaS 界面重构、14 个标准 Runbook 模板、分类筛选、指标总览、受控执行创建、计划执行持久化、执行触发源、撤销与重试闭环。

| 验收项 | 建议操作 | 预期结果 |
| --- | --- | --- |
| Runbook 目录 | 打开“标准化 Runbook 中心”，切换五个分类筛选 | 指标显示 14 个模板；风险徽章、图标与悬浮快捷入口清晰可用。 |
| 创建执行单 | 选择模板，填写实例/节点/参数，分别尝试立即与计划执行 | 立即执行进入审批或队列；未来时间创建的任务记录为 `scheduled`，不会即时派发。 |
| 执行追踪 | 在数据表中使用日志、撤销与重试操作 | 撤销记录审计日志；失败或已撤销任务会创建一条新的重试执行单。 |
| 本地管理员 | 使用安全设置的初始化凭据登录，输入当前临时密码和符合策略的新密码 | 首次访问会强制改密；缺少或错误的当前临时密码会被拒绝；改密后旧会话失效并进入控制台。 |

## 发布步骤

已完成的版本检查包括 `pnpm check`、`pnpm test`（14 个测试文件、36 项测试）与 `pnpm build`。平台托管环境的正式发布需要由项目负责人在管理界面中点击 **Publish**。发布后，请以授权账户重复以上验收流程，并为计划执行配置托管 Heartbeat 或已联调的 XXL-Job 调度流程；开发服务器不会使用进程内计时器触发计划任务。
